name: 'mini batch'
train_test_split:
  # can be any attribute of the BatteryData object. Values listed here will be EXCLUDED.
  battery_filters: 
    organization:
      - 'calce'
      - 'oxford'
      - 'hnei'
      - 'snl'
    cathode_material:
      - 

  # any Timeseries data with any of the following terms in its description will be EXCLUDED.
  timeseries_filter: 
    - 'punch'